# fingerprint-damper
Firefox extension that blunts browser fingerprinting at the JavaScript API level. Uses stable per-origin spoofing instead of random noise, so sites don't break. Complements uBlock Origin / Privacy Badger rather than duplicating them.
